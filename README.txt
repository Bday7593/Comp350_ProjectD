In this project I created functions within shell and kernel that would write to a sector, 
list the contents of the directory, delete a file, write a file, copy a file and create a text file. 
All of these functions work properly and can be implemented in the simulator. 